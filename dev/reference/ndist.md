# Calculate the number of distributions in a `<dist_spec>`

Calculate the number of distributions in a `<dist_spec>`

## Usage

``` r
ndist(x)
```

## Arguments

- x:

  A `<dist_spec>` object.

## Value

The number of distributions.
