# Utilities for CmdStanR
# Internal Function for transcribing rstan arguments to those compatible with
# cmdstanr

create_stancmd_args <- function(stan_args){
  
  
}

initialise_cmdstan_model <- function(model){
}


input <- create_stan_args("cmdstan")
