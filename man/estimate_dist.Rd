% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_dist_prototype.R
\name{estimate_dist}
\alias{estimate_dist}
\title{Estimate a delay distribution (PROTOTYPE)}
\usage{
estimate_dist(
  data,
  dist = "lognormal",
  primary_dist = "uniform",
  samples = 2000,
  chains = 4,
  cores = 1,
  max_value,
  verbose = FALSE,
  backend = "cmdstanr",
  ...
)
}
\arguments{
\item{data}{Either:
\itemize{
\item A numeric vector of delay values (for simple case)
\item A data.frame with linelist data containing delay observations
}}

\item{dist}{Character string, which distribution to fit. Supported options:
\itemize{
\item "lognormal" (default)
\item "gamma"
\item "weibull" (if supported by primarycensored)
}}

\item{primary_dist}{Character, the primary distribution to use. Default is
"uniform" for daily censoring.}

\item{samples}{Numeric, number of posterior samples. Defaults to 2000.}

\item{chains}{Numeric, number of MCMC chains. Defaults to 4.}

\item{cores}{Numeric, number of cores to use. Defaults to 1.}

\item{max_value}{Numeric, maximum delay value. If not provided, inferred
from data.}

\item{verbose}{Logical, print progress messages? Defaults to FALSE.}

\item{backend}{Character, which Stan backend to use. Must be "cmdstanr"
for primarycensored support. Defaults to "cmdstanr".}

\item{...}{Additional arguments passed to fitting functions.}
}
\value{
A \verb{<dist_spec>} object summarising the fitted distribution.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Estimate a delay distribution from observed data with support for right
truncation and primary event censoring using the primarycensored package.
}
\details{
This function provides a modern interface for estimating delay distributions
that properly accounts for:
\itemize{
\item Primary event censoring (e.g., daily reporting of symptom onset)
\item Secondary event censoring (e.g., daily reporting of case notification)
\item Right truncation (observations may be truncated by observation time)
}

The function uses the primarycensored package which implements the methods
described in Park et al. (2024) and Charniga et al. (2024).
}
\examples{
\donttest{
# Simple vector input (backwards compatible with estimate_delay)
delays <- rlnorm(500, log(5), 1)
dist <- estimate_dist(delays, samples = 1000)

# Linelist data with truncation
# linelist <- data.frame(
#   ptime_lwr = sample(0:10, 100, replace = TRUE),
#   stime_lwr = sample(5:20, 100, replace = TRUE),
#   obs_time = 30
# )
# dist <- estimate_dist(linelist, dist = "gamma", samples = 1000)
}
}
\references{
Park, S. W., et al. "Estimating epidemiological delay distributions for
infectious diseases", \emph{medRxiv}, 2024.
\doi{https://doi.org/10.1101/2024.01.12.24301247}
Charniga, K., et al. "Best practices for estimating and reporting
epidemiological delay distributions of infectious diseases using public
health surveillance and healthcare data", \emph{arXiv e-prints}, 2024.
\doi{10.48550/arXiv.2405.08841}
}
