% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_dist.R
\name{estimate_dist}
\alias{estimate_dist}
\title{Estimate a delay distribution using primarycensored}
\usage{
estimate_dist(
  data,
  dist = "lognormal",
  priors = if (dist == "lognormal") {
     list(meanlog = Normal(1, 1), sdlog =
    Normal(0.5, 0.5))
 } else {
     list(shape = Normal(2, 2), rate = Normal(0.5, 0.5))

    },
  primary = "uniform",
  primary_params = numeric(0),
  stan = stan_opts(),
  max_value = NULL,
  obs_time_threshold = 2,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame with date columns:
\itemize{
\item \code{pdate_lwr} (required): lower bound of primary event date
\item \code{pdate_upr} (optional): upper bound of primary event date
(default: \code{pdate_lwr + 1})
\item \code{sdate_lwr} (required): lower bound of secondary event
date
\item \code{sdate_upr} (optional): upper bound of secondary event
date (default: \code{sdate_lwr + 1})
\item \code{obs_date} (optional): observation/censoring date
(default: \code{max(sdate_upr)})
\item \code{n} (optional): observation count/weight (default: 1)
}}

\item{dist}{Character string, which distribution to fit.
Currently only \code{"lognormal"} (default) and \code{"gamma"} are
supported.}

\item{priors}{A list of \verb{<dist_spec>} objects specifying priors
for the distribution parameters.
Names must match the parameters of the chosen distribution.
Defaults depend on \code{dist}:
\itemize{
\item lognormal:
\code{list(meanlog = Normal(1, 1), sdlog = Normal(0.5, 0.5))}
\item gamma:
\code{list(shape = Normal(2, 2), rate = Normal(0.5, 0.5))}
}}

\item{primary}{Character string specifying the primary event
distribution. One of:
\itemize{
\item \code{"uniform"} (default): uniform distribution over the
primary window
\item \code{"expgrowth"}: exponential growth distribution.
Requires \code{primary_params} to supply a fixed growth rate.
}}

\item{primary_params}{Numeric vector of parameters for the
primary distribution.
Only used when \code{primary = "expgrowth"}, in which case it
should be a single numeric value for the growth rate.
The growth rate is passed as fixed data to Stan, not
estimated.}

\item{stan}{A list of stan options as generated by \code{\link[=stan_opts]{stan_opts()}}. Defaults
to \code{\link[=stan_opts]{stan_opts()}}. Can be used to override \code{data}, \code{init}, and \code{verbose}
settings if desired.}

\item{max_value}{Numeric, maximum delay value for PMF.
If not provided, inferred from data.}

\item{obs_time_threshold}{Numeric, multiplier for the
obs-time-to-Inf heuristic. Observations where
\code{relative_obs_time > max(delay_upr) * obs_time_threshold}
are treated as untruncated. Default 2, following
\code{epidist}. Set to \code{Inf} to disable.}

\item{verbose}{Logical, print progress messages?
Defaults to FALSE.}
}
\value{
An \verb{<estimate_dist>} object (inheriting from
\verb{<epinowfit>}) with components:
\describe{
\item{fit}{The Stan fit object.}
\item{args}{The Stan data list used for fitting.}
\item{data}{The input data.}
}
Use \code{\link[=get_parameters]{get_parameters()}} to extract the fitted \verb{<dist_spec>}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Fit a delay distribution accounting for primary and secondary
event censoring (double interval censoring) and right
truncation.
Estimation is done via MCMC using a Stan model that vendors
likelihood functions from the
\link[primarycensored:primarycensored-package]{primarycensored}
package.
For more flexible delay distribution modelling (e.g.
time-varying delays, partial pooling, or regression on
covariates), see the
\href{https://epidist.epinowcast.org/}{epidist} package.
If you use this function, please cite \code{primarycensored} in
addition to \code{EpiNow2}.
}
\details{
The model fits an interval-censored delay distribution while
accounting for:
\itemize{
\item Primary event censoring (e.g., daily reporting of exposure)
\item Secondary event censoring (e.g., daily reporting of symptom
onset)
\item Right truncation (observation window effects)
\item Per-observation truncation times (via \code{obs_date})
}

When a data frame with date columns is provided, observations
are aggregated by unique combinations of \verb{(delay_lwr, delay_upr, pwindow, relative_obs_time)} to reduce the number
of likelihood evaluations.
Observations where the relative observation time is much
larger than the maximum observed delay are treated as
untruncated (observation time set to infinity).

The \code{primarycensored} Stan functions are vendored (included
in the package), so the model is pre-compiled and runs without
needing \code{primarycensored} at runtime.
\subsection{Limitations}{
\itemize{
\item Only lognormal and gamma delay distributions are currently
supported.
\item The primary event distribution is limited to uniform or
exponential growth with a fixed rate.
Primary event parameters are not estimated.
\item Left truncation is not yet exposed (internally set to 0).
}
}
}
\examples{
\donttest{
# Fit lognormal distribution from date-based linelist
if (requireNamespace("primarycensored", quietly = TRUE)) {
  set.seed(1)
  n <- 100
  D <- 30
  pdate_lwr <- as.Date("2023-01-01") + rpois(n, 5)
  delays_sim <- primarycensored::rprimarycensored(
    n = n, rdist = rlnorm,
    meanlog = log(5), sdlog = 0.5,
    pwindow = 1, D = D
  )
  linelist <- data.frame(
    pdate_lwr = pdate_lwr,
    sdate_lwr = pdate_lwr + delays_sim,
    obs_date = pdate_lwr + D
  )
  result <- estimate_dist(linelist, dist = "lognormal")
}
}
}
\references{
Park SW, et al. (2024) "Estimating epidemiological delay
distributions for infectious diseases."
doi:10.1101/2024.01.12.24301247

Charniga K, Park SW, et al. (2024) "Best practices for
estimating and reporting epidemiological delay
distributions of infectious diseases."
PLoS Comput Biol 20(10): e1012520.
doi:10.1371/journal.pcbi.1012520

Please cite \code{primarycensored} if you use this function;
see \code{citation("primarycensored")}.
}
\seealso{
\link[primarycensored:primarycensored-package]{primarycensored::primarycensored-package} for the
underlying censoring methodology.
}
