% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_forecast_sample.R
\name{as_forecast_sample}
\alias{as_forecast_sample}
\alias{as_forecast_sample.estimate_infections}
\alias{as_forecast_sample.epinow}
\alias{as_forecast_sample.forecast_secondary}
\alias{as_forecast_sample.estimate_truncation}
\title{Convert EpiNow2 model output to a \code{forecast_sample} object}
\usage{
\method{as_forecast_sample}{estimate_infections}(data, observations, ...)

\method{as_forecast_sample}{epinow}(data, observations, ...)

\method{as_forecast_sample}{forecast_secondary}(data, observations, ...)

\method{as_forecast_sample}{estimate_truncation}(data, observations, ...)
}
\arguments{
\item{data}{Output of \code{\link[=epinow]{epinow()}}, \code{\link[=estimate_infections]{estimate_infections()}},
\code{\link[=forecast_secondary]{forecast_secondary()}}, or \code{\link[=estimate_truncation]{estimate_truncation()}}.}

\item{observations}{A \verb{<data.frame>} of observed values to score against.
Must contain a \code{date} column. For \code{\link[=epinow]{epinow()}} and \code{\link[=estimate_infections]{estimate_infections()}}
objects must also contain a \code{confirm} column; for \code{\link[=forecast_secondary]{forecast_secondary()}}
objects a \code{secondary} column; for \code{\link[=estimate_truncation]{estimate_truncation()}} objects a
\code{confirm} column representing the latest, least-truncated observations.}

\item{...}{Additional arguments passed to
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}. For \code{\link[=estimate_truncation]{estimate_truncation()}} objects
any user-supplied \code{forecast_unit} must include \code{"dataset"}, since
predictions contain one observation snapshot per dataset and \code{sample_id}
values repeat across datasets.}
}
\value{
A \code{forecast_sample} object as returned by
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}. Rows for which \code{observations} does
not provide a value on the corresponding \code{date} are dropped.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Convert outputs of EpiNow2 fitting and forecasting functions to
\code{forecast_sample} objects from the \code{scoringutils} package for evaluating
predictive performance. Methods are provided for objects returned by
\code{\link[=epinow]{epinow()}}, \code{\link[=estimate_infections]{estimate_infections()}}, \code{\link[=forecast_secondary]{forecast_secondary()}}, and
\code{\link[=estimate_truncation]{estimate_truncation()}}.

These methods extract sample-level posterior predictions via
\code{\link[=get_predictions]{get_predictions()}} with \code{format = "sample"}, merge them with the supplied
observations on \code{date}, and pass the result to
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}.

\code{scoringutils} is an optional dependency. Methods are registered via
delayed S3 method registration so that EpiNow2 can be loaded and checked
without \code{scoringutils} installed; the methods themselves require it.
}
\examples{
\dontshow{if (interactive() && rlang::is_installed("scoringutils")) withAutoprint(\{ # examplesIf}
library(scoringutils)

reported_cases <- example_confirmed[1:60]
fit <- estimate_infections(
  reported_cases,
  generation_time = gt_opts(example_generation_time),
  delays = delay_opts(example_incubation_period + example_reporting_delay),
  rt = rt_opts(prior = LogNormal(mean = 2, sd = 0.2)),
  forecast = forecast_opts(horizon = 7)
)

forecast_obj <- as_forecast_sample(fit, observations = example_confirmed)
score(forecast_obj)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_predictions]{get_predictions()}} for the underlying sample extraction.
}
