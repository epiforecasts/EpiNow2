% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{create_state_data}
\alias{create_state_data}
\title{Create time-varying state data for stan}
\usage{
create_state_data(params, state_flags, states_supported = character(0))
}
\arguments{
\item{params}{A list of \verb{<EpiNow2.params>} after \code{NULL} parameters have been
removed, so that positions match the stan parameter ids.}

\item{state_flags}{Logical vector flagging which entries of \code{params} carry
a state specification.}

\item{states_supported}{Character vector of parameter names the calling model
can consume a time-varying state for. A state on any other parameter errors.}
}
\value{
A named list of stan data items describing the states.
}
\description{
Builds the minimal configuration the stan model needs to layer a stochastic
state on top of a parameter's level (see \code{\link[=create_stan_params]{create_stan_params()}}). The
trajectory length and centring window are derived in stan from the modelled
time, so only the state type, link, target parameter and step standard
deviation prior are emitted here.
}
\keyword{internal}
