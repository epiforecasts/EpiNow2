% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{build_np_est_data}
\alias{build_np_est_data}
\title{Build Stan data for estimated nonparametric delays}
\usage{
build_np_est_data(np_delays, np_pmf_groups)
}
\arguments{
\item{np_delays}{A list of nonparametric \code{dist_spec} objects in
their original order. Each estimated entry carries a Dirichlet
prior (its \verb{$pmf} is a \code{dist_spec}) whose \code{alpha} is aligned
with its PMF.}

\item{np_pmf_groups}{Integer vector of 1-indexed PMF group
boundaries (output of \code{create_stan_delays()}).}
}
\value{
A named list with \code{n_np_est}, \code{np_est_which},
\code{np_est_alpha}, \code{np_est_pos}, \code{np_est_groups}, and
\code{np_est_length}. Empty arrays are returned when no delays are
estimated.
}
\description{
Takes the nonparametric delays and the PMF group boundaries and
returns the ragged-array fields that Stan needs to map a
normalised Dirichlet draw back into \code{delay_np_pmf}.
}
\details{
Structural zeros (entries where the prior alpha is zero, e.g. the
\code{t = 0} generation-time bin) are dropped from the estimated
parameter vector so Stan never sees a \code{Gamma(0, 1)} rate. Their
positions are still held by the fixed \code{delay_np_pmf} entries and
are left untouched at sampling time.
}
\keyword{internal}
