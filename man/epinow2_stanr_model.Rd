% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan.R
\name{epinow2_stanr_model}
\alias{epinow2_stanr_model}
\title{Load an EpiNow2 stanr model}
\usage{
epinow2_stanr_model(
  model = "estimate_infections",
  dir = system.file("stan", package = "EpiNow2"),
  backend = c("compiled", "stanli"),
  verbose = FALSE
)
}
\arguments{
\item{model}{A character string indicating the model to use. Needs to be
present in \code{dir} (with extension \code{.stan}). Defaults to "estimate_infections".}

\item{dir}{A character string specifying the path to any stan
files to include in the model. If missing the package default is used.}

\item{backend}{A character string giving the \code{stanr} model backend to use,
either "compiled" (default) or "stanli".}

\item{verbose}{Logical, defaults to \code{TRUE}. Should verbose
messages be shown.}
}
\value{
A \code{StanModel} object.
}
\description{
Builds a model with \code{\link[stanr:stan_model]{stanr::stan_model()}}. The \code{compiled} backend
translates the model to C++ and compiles it, caching the result. The
\code{stanli} backend interprets the model over precompiled kernels instead, so
it needs no C++ toolchain and preparing a model takes milliseconds.
}
