% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_delay}
\alias{get_delay}
\alias{get_delay.default}
\title{Get a single delay distribution from a fitted model}
\usage{
get_delay(object, type, ...)

\method{get_delay}{default}(object, type, ...)
}
\arguments{
\item{object}{A fitted model object (e.g., from \code{estimate_infections()},
\code{estimate_secondary()}, or \code{estimate_truncation()})}

\item{type}{Character string specifying the delay type to extract.
Common values include \code{"generation_time"}, \code{"reporting"}, and
\code{"truncation"}. Use \code{names(get_delays(object))} to see available types.}

\item{...}{Additional arguments passed to methods}
}
\value{
A \code{dist_spec} object representing the requested delay distribution,
or \code{NULL} if the specified type is not found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Convenience function to extract a single delay distribution by name.
This is equivalent to \code{get_delays(object)[[type]]} but provides a
cleaner interface.
}
\examples{
\dontrun{
# Get truncation delay from estimate_truncation
trunc_dist <- get_delay(fit, "truncation")

# Get generation time from estimate_infections
gt <- get_delay(fit, "generation_time")
}
}
\seealso{
\code{\link[=get_delays]{get_delays()}} to retrieve all delay distributions as a list
}
