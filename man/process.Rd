% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{process}
\alias{process}
\alias{GP}
\alias{RW}
\title{Time-varying parameter processes}
\usage{
GP(mean, init, ...)

RW(mean, init, sd = Normal(mean = 0, sd = 0.1))
}
\arguments{
\item{mean}{A \verb{<dist_spec>} giving the prior on the (stationary) mean the
process reverts to, or a numeric vector giving a known mean trajectory the
process fits deviations around. Supply either \code{mean} or \code{init}, not both.}

\item{init}{A \verb{<dist_spec>} giving the prior on the initial value of a process
on first differences, or a numeric vector giving known initial value(s).
Supply either \code{mean} or \code{init}, not both.}

\item{...}{Additional Gaussian process settings passed to \code{\link[=gp_opts]{gp_opts()}} (e.g.
\code{ls}, \code{alpha}, \code{kernel}).}

\item{sd}{A \verb{<dist_spec>} giving the prior on the random walk step standard
deviation. Defaults to a half-normal \code{Normal(mean = 0, sd = 0.1)} (the lower
limit of 0 is enforced where the parameter is used).}
}
\value{
A \verb{<process_spec>} object describing the time-varying process.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

These constructors mark a model parameter as varying over time, driven by a
stochastic process. \code{GP()} uses an approximate Gaussian process and \code{RW()} a
random walk. They wrap a \verb{<dist_spec>} giving the prior on the value the
parameter reverts to (\code{mean}, a stationary / mean-reverting process) or on its
initial value (\code{init}, a process on first differences). Exactly one of \code{mean}
or \code{init} must be supplied.

The wrapped prior may itself be a known trajectory (e.g. a \code{NonParametric()}
built from a data column), in which case the process fits deviations around
that mean. The link function applied to the resulting trajectory (e.g. log for
positive parameters, logit for probabilities) is a property of the parameter
and is set where the parameter is registered, not here.
}
\details{
The process specification deliberately carries only the value/process part of
a time-varying parameter. The level (whether the overall value is estimated or
\code{Fixed()}) and the link function are handled where the parameter is used.

These functions define the user interface for time-varying parameters. Wiring
them into the models is ongoing; passing a process specification where a model
parameter is expected currently raises an informative error.
}
\examples{
# mean-reverting Gaussian process
GP(mean = Normal(mean = 5, sd = 1))
# Gaussian process on first differences
GP(init = Normal(mean = 5, sd = 1))
# random walk with an initial-value prior
RW(init = Normal(mean = 5, sd = 1))
# mean-reverting random walk with a custom step size prior
RW(mean = Normal(mean = 5, sd = 1), sd = Normal(mean = 0, sd = 0.05))
}
